// Events => actions or occurances in the browser

// event => object => details

// (input) => element;

// object => event.target.value

// events
/* 
// 1. mouse events
click;
dbclick;
mouseover;
mouseout;
mousemove;
mouseDown;
mouseup;
click
// 2. keyboard events
keydown;
keyup;
keyPress;

// form events
submit;
change;
focus;

// window events

load;
resize;
scorll;
 */

// event is an object in js => information about an event
// automatically passed to event listerners / handlers when an event is occur
// proprties and methods thar provides details about the events. type, triggered it, values, etc

// (type) => click, dbclick, submit, change;
// target => element that triggered the event

// preventDefault()
